This folder holds temporary "pending approval" news items generated by the
daily automation (scripts/fetch-and-notify.mjs) and consumed by the Telegram
approval webhook (netlify/functions/telegram-webhook.js). It is normal for
this folder to be empty most of the time — files appear here briefly between
a news item being found and Masoud approving or rejecting it on Telegram.
